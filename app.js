// Todays weather forecast //
const kelvin = 393;
let celsius = 20;
// Fahrenherit Calculations//
let newton = celsius * (33/100)
/* this line of code will convert celsius to fahrenheit*/
fahrenheit = Math.floor(newton);
/* at this point console will help us print our results to the console*/
console.log(`The temperature is ${newton} degrees Newton`);